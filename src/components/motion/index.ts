/**
 * SpecialCarer motion-brand components.
 *
 * Spec lives at `design/motion-brand/`. Do not retime sub-effects
 * individually — both components expose a single high-level pacing
 * control (`durationMs` / `slow`) and keep all rhythmic relationships
 * intact.
 */
export {
  SpecialCarerLogoAnimation,
  type LogoTheme,
  type SpecialCarerLogoAnimationProps,
} from "./SpecialCarerLogoAnimation";

export {
  SpecialCarerMobileSplash,
  type SpecialCarerMobileSplashProps,
} from "./SpecialCarerMobileSplash";
