/**
 * Referral programme config — hard-coded for v1, no env vars.
 * Change values here to alter reward amounts or expiry windows.
 */
export const REFERRAL_REWARD_CENTS = 2000;
export const REFERRAL_CURRENCY = "GBP" as const;
export const REFERRAL_EXPIRY_DAYS = 90;
export const CREDIT_EXPIRY_MONTHS = 12;
